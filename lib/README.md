# v86 runtime files

Place these files here from your v86 build or deployment:

- `libv86.js`
- `v86.wasm`

The shared template loads `lib/libv86.js`, and `js/vm-init.js` points v86 to `lib/v86.wasm`.
